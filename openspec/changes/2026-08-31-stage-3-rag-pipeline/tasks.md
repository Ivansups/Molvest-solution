> Статус 2026-08-31: реализовано по исходной постановке (см. proposal) и проверено.
> Секция 6 «Переработка под новый план» (Redis-кэш, classify правилами, скор ретривала,
> эскалация без generate, фоновый reindex) также реализована и проверена ниже.

## 1. Извлечение текста и чанкинг

- [x] 1.1 Добавить `pypdf` и `python-docx` в зависимости сервера и зафиксировать их в uv.lock — проверка: `cd server && uv sync --frozen` успешен
- [x] 1.2 Реализовать извлечение текста из PDF, DOCX, HTML, MD (неблокирующее) — проверка: юнит-тест извлекает текст из образца каждого поддерживаемого типа
- [x] 1.3 Реализовать чанкер по словам с `max_chunk_size` и `chunk_overlap` (настраиваемые из конфига) — проверка: юнит-тест: короткий текст → 1 чанк; длинный → несколько с общим перекрытием
- [x] 1.4 Покрыть поведение из спека `rag-ingestion` "Document is split into word-based chunks with overlap" тестом — проверка: `uv run pytest tests/test_rag.py` зелёный

## 2. Индексация и атомарный reindex

- [x] 2.1 Реализовать `index_document`: чтение + извлечение off the event loop, затем чанкинг и эмбеддинги ДО открытия транзакции — проверка: юнит-тест подтверждает, что эмбеддинги вызываются до записи чанков
- [x] 2.2 Реализовать атомарную замену чанков: удалить старые + вставить новые + установить статус `INDEXED` в одной транзакции — проверка: тест на повторный reindex не оставляет ни старых, ни частично обновлённых чанков
- [x] 2.3 При ошибке извлечения/эмбеддинга помечать документ failed-статусом и не оставлять частичных чанков — проверка: негативный тест с падающим embeddings-провайдером
- [x] 2.4 Перевести `POST /api/documents/{id}/reindex` с no-op на реальный reindex (spec `knowledge-base-documents` "Reindex rebuilds document chunks") — проверка: API-тест reindex возвращает документ со статусом `INDEXED` и чанками
- [x] 2.5 Убедиться, что повторный reindex идемпотентен и при неудаче отдаёт ошибку без частичного состояния — проверка: тест вызывает reindex дважды и проверяет failed-сценарий

## 3. Ретривал

- [x] 3.1 Реализовать `retrieve_chunks`: эмбеддинг запроса + поиск по `cosine_distance` в `pgvector`, `LIMIT top_k` — проверка: тест возвращает ближайший чанк по эмбеддингам
- [x] 3.2 Ограничить ретривал чанками `INDEXED`-документов текущей установки (spec `rag-retrieval` "Retrieval is scoped to one installation" / "Only indexed documents are searchable") — проверка: тест: чужой installation и неindexed документы не попадают в результат
- [x] 3.3 Реализовать детерминированный маппинг `workspace_id -> installation_id` — проверка: юнит-тест: один и тот же workspace_id всегда даёт один installation_id
- [x] 3.4 Реализовать `make_retriever` и `EmbeddingsProvider` протокол для подмены эмбеддингов — проверка: ретривер инжектится с фейк-эмбеддером в тестах без сети

## 4. Граф агента

- [x] 4.1 Добавить узел `retrieve` в граф LangGraph и передавать найденные чанки в ответ — проверка: тест графа: при наличии чанков ответ имеет sources, при отсутствии — пустые sources (spec `rag-retrieval` "Agent receives retrieved context for generation")
- [x] 4.2 Прокинуть `installation_id` в состояние агента из `workspace_id` на входе — проверка: тест графа подтверждает корректное состояние
- [x] 4.3 Прогнать `cd server && uv run ruff check . && uv run ruff format --check . && uv run mypy .` — проверка: все три чистые

## 5. Интеграционная проверка

- [x] 5.1 Прогнать полный набор тестов на реальном Postgres (`TEST_DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/molvest uv run pytest`) — проверка: все тесты зелёные
- [x] 5.2 Проверить `cd server && uv run alembic check` — проверка: дрейфа миграций нет

## 6. Переработка под новый план (docs/ROADMAP.md из main)

- [x] 6.1 Добавить Redis в docker-compose и `REDIS_URL` в конфиг/.env — проверка: `docker compose up` поднимает Redis, приложение читает `REDIS_URL`
- [x] 6.2 Кэш эмбеддингов запроса: ключ `emb:{hash(нормализованный текст)}`, TTL ~1ч, не сбрасывается при реиндексе — проверка: тест: повторный текст берётся из кэша
- [x] 6.3 Счётчик `kb_version` в Redis: инкремент при успешной индексации/удалении документа — проверка: тест: reindex увеличивает `kb_version`
- [x] 6.4 Кэш готовых ответов `ans:{kb_version}:{hash(вопрос)}` — проверка: тест: повтор вопроса при той же `kb_version` берётся из кэша
- [x] 6.5 `classify` без LLM: правила (приветствие/благодарность/пустое/оффтоп) вместо вызова GigaChat — проверка: тест: приветствие и пустой ввод не вызывают модель
- [x] 6.6 Уверенность = скор лучшего чанка (убрать узел `confidence_check` с вызовом модели) — проверка: тест: confidence берётся из score ретривала
- [x] 6.7 Эскалация без generate: если скор лучшего чанка < порога (`CONFIDENCE_THRESHOLD`) — escaled, без вызова модели — проверка: тест: слабый скор даёт escalation и `generate` не вызван
- [x] 6.8 Ответ агенту «документ принят» не ждёт эмбеддинги (индексация через FastAPI `BackgroundTasks`) — проверка: тест/ручная проверка: upload возвращается до завершения индексации
