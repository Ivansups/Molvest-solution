# Техническое задание на разработку AI-агента для техподдержки 1С (Хакатон «Молвест»)

## 1. Назначение документа

Данный документ – единый план реализации AI-агента (чат-бота) для автоматизации технической поддержки пользователей 1С. Он предназначен для команды разработчиков (3 человека) и фиксирует:

- согласованную архитектуру и стек;
- приоритетные сценарии и порядок их реализации;
- функциональные требования к каждому модулю;
- контракты взаимодействия между компонентами;
- критерии оценки (в соответствии с правилами хакатона);
- обязательные тесты и критерии завершения каждого этапа.

Разработка ведётся итеративно, строго по этапам. Переход к следующему этапу допустим только после выполнения критериев готовности текущего.

## 2. Область реализации

Система представляет собой **веб-приложение** (монолит с чётким разделением на бэкенд, фронтенд и агентный слой), которое предоставляет:

- **чат-интерфейс** для пользователей (в виде веб-виджета, имитирующего интеграцию с Bitrix24/Redmine);
- **административную панель** для управления базой знаний, просмотра логов и метрик;
- **интеллектуальное ядро** на базе GigaChat (RAG, классификация запросов, генерация ответов, анализ скриншотов, оценка уверенности);
- **механизм эскалации** операторам при низкой уверенности.

### 2.1. Что входит в реализацию

- Обработка текстовых запросов пользователей с поиском по базе знаний (RAG).
- Анализ загруженных изображений (скриншотов 1С) с использованием GigaChat Vision.
- Генерация ответов с указанием источника (документ БЗ).
- Оценка уверенности GigaChat в ответе; при пороге < 80% – эскалация (имитация передачи оператору).
- Интерфейс администратора для загрузки/редактирования/удаления документов БЗ (Word, PDF, HTML, Markdown).
- Просмотр истории диалогов и метрик (процент автоответов, среднее время ответа, количество эскалаций).
- Упрощённая имитация интеграции с Bitrix24 и Redmine (для демонстрации – вебхуки или заглушки).
- **Авторизация администратора** — на стороне Next.js через **NextAuth**, хранение пользователей/сессий через **Prisma** (см. п. 3.1, 3.4).

### 2.2. Что не реализуется

- Полноценная интеграция с реальными системами Bitrix24/Redmine (делается упрощённо, через моки).
- Хранение больших объёмов данных (тестовое наполнение БЗ предоставляется командой).

## 3. Согласованная архитектура

### 3.1. Технологический стек

- **Backend**: Python 3.12+, FastAPI, SQLAlchemy (async), Alembic.
- **База данных**: PostgreSQL 15+ с расширением pgvector.
- **Агентный слой**: LangGraph – оркестрация сценариев (вопрос-ответ, анализ изображений, эскалация).
- **AI-модель**: GigaChat (API) – используется для эмбеддингов, генерации, Vision, оценки уверенности.
- **Frontend**: React (Next.js) с Tailwind CSS.
- **Авторизация**: NextAuth.js (на стороне Next.js) + **Prisma** — Prisma работает только с auth-таблицами NextAuth (`User`, `Account`, `Session`, `VerificationToken`) в той же PostgreSQL, не трогает домен-модели (Document, Chunk, Conversation, Message, Escalation), которыми управляет SQLAlchemy/Alembic.
- **Инфраструктура**: Docker Compose (FastAPI, PostgreSQL, Redis – опционально), GitHub, README с инструкцией развертывания.
- **GigaChat SDK**: `pip install gigachat` — официальная Python-библиотека, используется и для обычного чата, и для Vision, и для эмбеддингов (один SDK на все режимы).
- **API-стиль Next.js ↔ FastAPI**: обычный **REST** (JSON через `fetch`/`axios`), не tRPC. На дедлайне хакатона это надёжнее и быстрее в реализации; типизация закрывается через OpenAPI-схему FastAPI (`drf-spectacular`-аналог из коробки — `/docs`, `/openapi.json`). tRPC даёт полную типобезопасность, но требует лишней настройки на старте — не оправдано для 7 дней.

### 3.2. Взаимодействие компонентов

- **Пользователь** ↔ **React-виджет** ↔ **FastAPI (синхронно, REST)**.
- **FastAPI** ↔ **LangGraph** (внутренний вызов).
- **LangGraph** ↔ **GigaChat API** (через единый клиент).
- **FastAPI** ↔ **PostgreSQL+pgvector** (CRUD документов, хранение чанков, эмбеддингов, диалогов, эскалаций).
- **Администратор** ↔ **React-админка** (защищена **NextAuth**-сессией) ↔ **FastAPI (REST)**.
- **Next.js-сервер** ↔ **Prisma** ↔ **PostgreSQL** (только auth-таблицы NextAuth).
- **Next.js-сервер** → **FastAPI**: доверенный service-to-service вызов (внутренний токен) после проверки NextAuth-сессии на стороне Next.js — сам FastAPI сессии NextAuth не разбирает.

### 3.3. Ключевые модули (приложения)

- **core** – конфигурация, логирование, клиент GigaChat, общие утилиты.
  - `GigaChatService` — единая сервис-обёртка над SDK `gigachat`, централизует авторизацию (токен/сертификаты) и обработку ошибок/таймаутов. Все остальные модули вызывают только её, не SDK напрямую:
    - `get_embeddings(texts: list[str]) -> list[list[float]]`
    - `chat_with_vision(image_bytes: bytes, prompt: str) -> str`
    - `generate(messages: list[Message]) -> str`
- **knowledge_base** – модели Document, Chunk; CRUD документов; индексация (чанкинг + эмбеддинги + сохранение в pgvector).
- **rag** – ретривал (поиск релевантных чанков), генерация ответа, проверка уверенности.
- **agent** – LangGraph-граф, объединяющий сценарии (текст, изображения, эскалация).
- **vision** – обработка скриншотов (извлечение текста и визуальной информации через GigaChat Vision).
- **conversations** – модели диалогов, сообщений, эскалаций; сервисы логирования и метрик.
- **channels** – адаптеры для имитации Bitrix24 и Redmine (веб-виджет, email-заглушка).
- **admin** – API и UI для управления БЗ и просмотра статистики.
- **auth (apps/web)** – NextAuth + Prisma; login-страница, middleware, защита admin-роутов. Живёт целиком на стороне Next.js, отдельно от Python-модулей.

### 3.4. Авторизация (NextAuth + Prisma)

- `apps/web/prisma/schema.prisma` — схема только для auth: `User`, `Account`, `Session`, `VerificationToken` (стандартная схема NextAuth Prisma Adapter).
- `apps/web/lib/auth.ts` — конфигурация NextAuth (провайдер credentials/email — на выбор фронтенд-разработчика), Prisma Adapter.
- `apps/web/middleware.ts` — защита `/admin/**` неавторизованным доступом.
- Миграции auth-схемы — через `prisma migrate`, независимо от `alembic` (разные наборы таблиц в одной БД, конфликтов имён нет).
- FastAPI не хранит пользователей и не валидирует пароли/токены NextAuth — только принимает внутренний service-токен от Next.js-сервера.

## 4. Порядок реализации этапов

Разработка ведётся параллельно тремя разработчиками, но строго по приоритетам:

1. **Инфраструктура и контракты** – настройка базы, конфигов, Docker, Alembic.
2. **Ядро RAG и чат-бота** – обязательный сценарий, основа оценки.
3. **Управление БЗ (админка)** – легко демонстрируется, даёт баллы за UI/UX.
4. **Анализ изображений** – повышает качество использования GigaChat.
5. **Интерфейсы (чат-виджет и админка)** – фронтенд.
6. **Интеграции (упрощённо)** – имитация Bitrix/Redmine.
7. **Бонусные функции** – развитие решения.

---

## Этап 1. Инфраструктура и общие контракты

### 1.1. Цель этапа

Создать работающую среду разработки, настроить базу данных, определить единый формат запросов/ответов между фронтендом и бэкендом.

### 1.2. Реализуемые задачи

- Настройка **Docker Compose** с PostgreSQL (pgvector), FastAPI, Redis (опционально).
- Создание **файла `.env`** с переменными: `GIGACHAT_API_KEY`, `GIGACHAT_API_URL`, `DATABASE_URL`, `CONFIDENCE_THRESHOLD` (0.8), `TOP_K` (5), `MAX_CHUNK_SIZE` (512), `NEXTAUTH_SECRET`, `NEXTAUTH_URL`, `INTERNAL_SERVICE_TOKEN` (Next.js → FastAPI), и т.д.
- Инициализация **Alembic** и создание базовых миграций для моделей (см. этап 2).
- Инициализация **Prisma** в `apps/web` со схемой NextAuth (`User`, `Account`, `Session`, `VerificationToken`), `prisma migrate dev` для первой миграции.
- Определение **единого контракта** для `POST /chat`:

```json
{
  "message_id": "uuid",
  "workspace_id": "string",         // для изоляции
  "conversation_id": "uuid | null", // если новый диалог – null
  "text": "string | null",
  "image_base64": "string | null",  // чистый base64, БЕЗ префикса data:image/png;base64,
  "user_id": "string"
}
```

> `image_base64` — сырая base64-строка, как её отдаёт `FileReader.readAsDataURL()` в браузере после обрезки префикса `data:image/...;base64,`. Бэкенд сам добавляет этот префикс обратно перед вызовом GigaChat Vision (см. Этап 3.5) — декодировать в байты и хранить как файл не нужно, если только не требуется постоянное хранение скриншота.

Ответ:

```json
{
  "conversation_id": "uuid",
  "message_id": "uuid",
  "text": "string",
  "confidence": 0.87,
  "escalated": false,
  "sources": [{"document_id": "uuid", "title": "string", "chunk_text": "..."}]
}
```

- Реализация **базового обработчика ошибок** и логирования (correlation_id, request_id).
- Написание **README.md** с инструкцией по запуску (`docker-compose up`).

### 1.3. Критерии завершения

- `docker-compose up` поднимает все сервисы без ошибок.
- `POST /health` возвращает `200 OK`.
- Alembic успешно применяет миграции.
- Prisma успешно применяет auth-миграции, `next-auth` login-flow работает локально.
- Контракт `POST /chat` задокументирован в README или OpenAPI.

---

## Этап 2. База знаний и хранение данных (Core + Knowledge Base)

### 2.1. Цель

Реализовать модели хранения документов, их чанков и эмбеддингов, а также базовые CRUD-операции для администрирования.

### 2.2. Модели данных

**Document**
- `id` (UUID)
- `installation_id` (UUID) – изоляция
- `title` (str)
- `file_name` (str)
- `file_type` (enum: PDF, DOCX, HTML, MD)
- `status` (enum: PENDING, INDEXED, FAILED)
- `uploaded_at`, `indexed_at`
- `metadata` (JSON)

**Chunk**
- `id` (UUID)
- `document_id` (FK)
- `content` (text)
- `embedding` (vector(1536)) – размерность эмбеддингов GigaChat
- `chunk_index` (int)

**Conversation**
- `id` (UUID)
- `installation_id`
- `user_id`
- `created_at`
- `status` (open, escalated, resolved)

**Message**
- `id` (UUID)
- `conversation_id`
- `role` (user, assistant, system)
- `content` (text)
- `image_url` (опционально)
- `confidence` (float)
- `escalated` (bool)
- `sources` (JSON – список {document_id, title, snippet})
- `created_at`

**Escalation**
- `id` (UUID)
- `conversation_id`
- `message_id`
- `reason` (text)
- `escalated_to` (string – оператор)
- `resolved_at` (null, если открыта)

### 2.3. Сервисы

- `upload_document(file, metadata)` – сохраняет файл, запускает фоновую индексацию (см. этап 3).
- `delete_document(document_id)` – удаляет документ и все его чанки.
- `list_documents()` – пагинированный список с фильтрами.
- `get_document(document_id)` – возвращает документ с чанками.
- `reindex_document(document_id)` – пересоздаёт чанки и эмбеддинги.

### 2.4. API для админки

```text
GET    /api/documents
POST   /api/documents
GET    /api/documents/{id}
DELETE /api/documents/{id}
POST   /api/documents/{id}/reindex
```

### 2.5. Тесты

- Загрузка PDF и DOCX, проверка сохранения.
- Удаление документа и каскадное удаление чанков.
- Повторная загрузка с тем же именем (обработка дублей).
- Проверка, что при удалении удаляются и эмбеддинги из pgvector.

### 2.6. Критерий завершения

- Все модели созданы и мигрированы.
- CRUD-операции работают через API (можно проверить через Swagger).
- Индексация ещё не реализована, но загрузка и удаление работают.

---

## Этап 3. RAG-пайплайн и использование GigaChat (Agent Core)

### 3.1. Цель

Реализовать интеллектуальное ядро: индексация документов, ретривал, генерация ответа с оценкой уверенности, эскалация.

### 3.2. Индексация (Ingestion)

- При загрузке документа или по команде `reindex`:
  - Разбить содержимое на чанки (размер – настраивается, 512 токенов, перекрытие 128).
  - Для каждого чанка получить эмбеддинг через `GigaChatService.get_embeddings()`.
  - Сохранить чанк и эмбеддинг в pgvector (использовать `INSERT ... ON CONFLICT`).
- Весь процесс выполняется асинхронно (запускается как Celery задача или через FastAPI BackgroundTasks – для упрощения на хакатоне достаточно `BackgroundTasks`).

**Про эмбеддинги GigaChat**: отдельный эндпоинт `POST /embeddings` в API GigaChat, тарифицируется отдельно от генерации текста — не забывать при оценке лимитов токенов (см. риски, п. 11). Доступно несколько моделей эмбеддингов; для проекта фиксируем одну модель на весь срок хакатона — смена модели эмбеддингов требует полной реиндексации базы знаний (размерность/пространство векторов меняется).

### 3.3. Ретривал

- Получить эмбеддинг запроса пользователя.
- Выполнить поиск по pgvector (`ORDER BY embedding <-> query_embedding LIMIT top_k`).
- Вернуть top_k чанков с текстом и метаданными.

### 3.4. Генерация и оценка уверенности (LangGraph)

- **Граф агента** (LangGraph) включает узлы:
  1. `classify` – определить, является ли запрос вопросом по 1С, или это приветствие/не по теме.
  2. `retrieve` – получить релевантные чанки.
  3. `generate` – сформировать системный промпт (задать роль эксперта 1С), передать контекст (чанки) и вопрос в GigaChat.
  4. `confidence_check` – попросить GigaChat оценить уверенность в ответе (шкала 0–1), используя дополнительный промпт.
  5. `route` – если уверенность >= порога (0.8) – ответ пользователю; иначе – эскалация (сохранить сообщение, создать Escalation, вернуть ответ "Передано оператору").
- Все вызовы GigaChat проходят через единый клиент (`core/gigachat_client`), который обрабатывает ошибки и таймауты.

### 3.5. Обработка изображений (скриншотов)

GigaChat Vision — мультимодальная модель: принимает текст + изображение, отдаёт текст. Отдельный OCR не нужен, модель сама читает текст/ошибки/интерфейс на скриншоте. Не требует S3 или файлового хранилища — картинка целиком прогоняется через API как текстовая (base64) строка внутри JSON, ничего заранее сохранять не нужно.

**Путь картинки по системе:**

1. **Next.js (клиент)** — файл из `<input type="file">` читается через `FileReader.readAsDataURL()`, результат `data:image/png;base64,iVBORw0KGgo...` — берём часть после запятой (чистый base64, без префикса) и кладём в поле `image_base64` контракта `POST /chat` (см. Этап 1.2).
2. **FastAPI** — принимает `image_base64` как обычную строку в Pydantic-модели запроса, ничего с ней не делает, кроме передачи дальше в `GigaChatService.chat_with_vision(...)`.
3. **`GigaChatService.chat_with_vision`** — перед вызовом SDK **добавляет обратно префикс** `data:image/png;base64,` (GigaChat ожидает полноценный data-URL, а не голую base64-строку) и формирует сообщение в формате контента с несколькими частями:
   ```python
   response = gc.chat({
       "messages": [{
           "role": "user",
           "content": [
               {"type": "text", "text": prompt},
               {"type": "image_url", "image_url": {
                   "url": f"data:image/png;base64,{image_base64}"
               }},
           ],
       }]
   })
   ```
4. **GigaChat → FastAPI** — модель возвращает обычный текстовый ответ (описание проблемы/рекомендация).
5. Полученный текст передаётся как часть запроса в тот же граф LangGraph (через узел `retrieve` – ищет по описанию проблемы). Изображение не образует отдельную ветку пайплайна — оно просто становится текстовым входом для общего RAG-пути (см. `docs/SCENARIOS.md`, сценарий 3).

**Промпт**: *«Опиши, что изображено на скриншоте, выдели ошибки, поля, коды. Если видишь ошибку 1С, объясни вероятную причину и предложи решение.»*

**Ограничения**:
- Размер изображения — не больше ~10–20 МБ; тяжёлые скриншоты сжимать на клиенте перед кодированием (canvas / `browser-image-compression`).
- Поддерживаемые форматы — PNG, JPEG, WEBP (проверить актуальный список в документации GigaChat).
- Обработка Vision может занимать несколько секунд — таймауты и на клиенте, и на бэкенде должны это учитывать (индикатор загрузки в виджете).
- Декодировать base64 обратно в файл на бэкенде нужно, только если решено постоянно хранить скриншот (например, для истории диалога) — для самого вызова Vision это не требуется.

### 3.6. Тесты

- Индексация документа: проверка создания чанков и эмбеддингов.
- Ретривал: поиск по чанкам возвращает релевантные.
- Генерация: ответ содержит ссылки на источники.
- Уверенность: для нечётких запросов оценка ниже порога.
- Анализ изображения: корректный вызов Vision и интеграция в граф.

### 3.7. Критерий завершения

- Через API можно загрузить документ, дождаться индексации (или запустить вручную).
- После индексации текстовый запрос возвращает осмысленный ответ с указанием источника и оценкой уверенности.
- При уверенности < 0.8 возвращается ответ с эскалацией (имитация).
- Изображение обрабатывается и даёт ответ.

---

## Этап 4. Управление базой знаний (Админ-интерфейс)

### 4.1. Цель

Создать интерфейс для администратора, позволяющий загружать, редактировать, удалять документы и просматривать статус индексации.

### 4.2. Реализуемый функционал

- **Аутентификация**: страница логина на NextAuth, `/admin/**` закрыт `middleware.ts` для неавторизованных.
- **Страница документов**:
  - Список документов (название, тип, статус, дата загрузки).
  - Кнопка "Загрузить документ" (диалоговое окно с выбором файла и вводом названия).
  - Кнопка "Реиндексация" для выбранного документа.
  - Кнопка "Удалить" с подтверждением.
- **Статус индексации** отображается (PENDING / INDEXED / FAILED) и обновляется без перезагрузки (опционально – вебсокеты или периодический опрос).

### 4.3. API (уже реализовано на этапе 2)

Используются готовые эндпоинты, добавлен эндпоинт для получения статуса индексации (можно включить в GET /documents).

### 4.4. Тесты

- Неавторизованный доступ к `/admin/**` редиректит на логин.
- Загрузка документа через UI, отображение в списке.
- Удаление документа – исчезает из списка.
- Реиндексация – статус меняется на INDEXED после завершения.

### 4.5. Критерий завершения

- Администратор логинится через NextAuth, попадает в защищённую админку.
- Администратор может загрузить документ и убедиться, что он проиндексирован.
- Интерфейс интуитивно понятен, соответствует критериям UX хакатона.

---

## Этап 5. Чат-виджет и пользовательский интерфейс

### 5.1. Цель

Создать веб-виджет для конечного пользователя, в котором можно задавать вопросы, загружать скриншоты и получать ответы с индикацией эскалации.

### 5.2. Функционал

- Поле ввода текста и кнопка "Отправить".
- Кнопка "Прикрепить изображение" (загрузка файла, предпросмотр).
- Отображение истории сообщений (пользователь – ассистент).
- Индикация эскалации: при уверенности < порога – сообщение "Вопрос передан оператору техподдержки".
- Отображение источников ответа (название документа, цитата) – можно сделать в виде ссылок или всплывающих подсказок.
- Адаптивный дизайн.

Виджет чата — публичный, без авторизации (конечные пользователи техподдержки не логинятся); NextAuth защищает только `/admin/**`.

### 5.3. Взаимодействие с бэкендом

- Использовать контракт `POST /chat` (этап 1), обычный REST/JSON (см. п. 3.1 — без tRPC).
- При загрузке изображения – кодировать файл в base64 на клиенте и передавать чистую строку (без `data:image/...;base64,` префикса) в поле `image_base64`:
  ```ts
  const toBase64 = (file: File): Promise<string> =>
    new Promise((resolve, reject) => {
      const reader = new FileReader();
      reader.readAsDataURL(file);
      reader.onload = () => resolve((reader.result as string).split(',')[1]);
      reader.onerror = reject;
    });
  ```
  Дальше — обычный `fetch('/api/chat', { method: 'POST', body: JSON.stringify({ text, image_base64 }) })`. Декодировать base64 обратно в файл на клиенте не нужно — бэкенд отдаёт готовый текстовый ответ.

### 5.4. Тесты

- Отправка текста, получение ответа.
- Отправка изображения, получение ответа.
- Проверка, что при эскалации отображается корректное сообщение.
- Проверка отображения источников.

### 5.5. Критерий завершения

- Виджет работает без ошибок, интерфейс понятен пользователю.
- Основной сценарий (вопрос → ответ) проходит гладко.

---

## Этап 6. Метрики и логи (Админ-дашборд)

### 6.1. Цель

Предоставить администратору инструмент для мониторинга эффективности агента.

### 6.2. Функционал

- **Дашборд** с метриками:
  - Процент автоответов (количество сообщений, не переданных оператору / общее количество).
  - Среднее время ответа (в секундах) – измерять от получения запроса до отправки ответа.
  - Количество эскалаций за период.
- **Логи диалогов**: список всех диалогов с фильтром по дате, пользователю, статусу (эскалирован/нет). При клике – детали диалога (все сообщения, оценка уверенности, источники).

### 6.3. API для метрик

- `GET /api/metrics` – возвращает агрегированные данные.
- `GET /api/conversations` – список диалогов с пагинацией и фильтрами.
- `GET /api/conversations/{id}` – детали.

### 6.4. Тесты

- Проверка расчёта процента автоответов.
- Проверка, что логи содержат все необходимые поля.
- Фильтрация работает корректно.

### 6.5. Критерий завершения

- Администратор видит дашборд с обновляемыми данными.
- Доступен просмотр истории диалогов.

---

## Этап 7. Интеграция с внешними каналами (упрощённо)

### 7.1. Цель

Продемонстрировать, как агент может подключаться к Bitrix24 и Redmine. В рамках хакатона реализуется упрощённая имитация.

### 7.2. Реализация

- **Bitrix24**: создать эндпоинт-вебхук, имитирующий входящее сообщение (POST /webhook/bitrix). При получении он вызывает основной обработчик `POST /chat` с параметрами, и возвращает ответ как если бы он отправлялся в Bitrix. Для демонстрации можно использовать тестовый портал или вовсе мок.
- **Redmine (email)**: реализовать простой IMAP-поллинг (или мок) для чтения писем с заданного ящика и отправки ответа по email. В целях хакатона это может быть просто симуляция через консоль.

### 7.3. Приоритет

Этот этап имеет наименьший приоритет. Если не хватает времени, можно ограничиться **демонстрацией через виджет**, а интеграцию описать в презентации как готовую, но не показывать вживую.

### 7.4. Критерий завершения (если реализуется)

- Вебхук принимает сообщение, возвращает корректный ответ.
- Email-обработчик показывает, что письмо прочитано и на него сформирован ответ (можно в логах).

---

## Этап 8. Бонусные функции (Развитие решения)

### 8.1. Цель

Предложить и реализовать (хотя бы частично) дополнительные возможности, повышающие ценность продукта для заказчика. Это даёт до 10 бонусных баллов.

### 8.2. Идеи для реализации (выбрать 1–2)

- **Автоматическое обучение на новых кейсах**: при эскалации оператор решает проблему, и решение заносится в БЗ (можно сделать через административный интерфейс «Добавить кейс»).
- **Прогнозирование частоты вопросов**: анализ логов для выявления самых частых проблем.
- **Генерация инструкции по запросу**: пользователь просит инструкцию в формате PDF – система генерирует и отдаёт.
- **Интеграция с телеграм-ботом** (как дополнительный канал) – можно реализовать простого бота на webhook.

### 8.3. Критерий

- Функция проработана и демонстрируется в действии (или хотя бы показан прототип).
- Чётко объяснена бизнес-ценность для заказчика.

---

## 9. Общие требования к качеству кода и документации (согласно критериям)

- Исходный код размещён в **публичном репозитории GitHub** (или доступен по ссылке для жюри).
- В репозитории есть **README.md** с полной инструкцией по развёртыванию (docker-compose, переменные окружения, миграции).
- Код содержит комментарии к сложным участкам (интеграция с GigaChat, логика эскалации, пайплайн обработки изображений).
- Реализовано логирование с корреляционными ID для трассировки.
- Проект должен быть доступен для тестирования в течение **10 рабочих дней** после окончания хакатона (желательно задеплоить на бесплатный хостинг, например, Render или Heroku, либо предоставить работающий локальный запуск через Docker).

---

## 10. Тестирование и критерии приемки этапов

Каждый этап считается завершённым, если:

1. Все перечисленные сервисы/функции реализованы и проходят позитивные и негативные тесты (unit / интеграционные).
2. Нет дублирования бизнес-логики в разных модулях.
3. Операции, изменяющие состояние, транзакционны (использование БД).
4. Создаются логи и (при необходимости) аудит.
5. API документированы (OpenAPI или в README).
6. Тесты проходят успешно.
7. Ветка кода соответствует структуре, описанной в `AGENTS.md` (если есть).
8. Следующий этап не начинает разрабатываться, пока текущий не сдан.

---

## 11. Риски и их смягчение

- **GigaChat API лимиты**: использовать кэширование эмбеддингов, не генерировать повторно при реиндексации без изменений.
- **Время ответа > 5 секунд**: использовать асинхронность, лёгкую модель (GigaChat Lite) для быстрых запросов, а для сложных – показывать индикатор загрузки.
- **Сложность интеграции Bitrix/Redmine**: делать упрощённо, оставляя ядро на виджете.
- **Отсутствие данных для БЗ**: наполнить тестовыми данными из предоставленных источников (инструкции, документация 1С).
- **Две ORM на одной БД (Prisma + SQLAlchemy)**: жёстко разграничены по таблицам (auth vs домен), миграции не пересекаются; не давать Prisma-схеме трогать домен-таблицы и наоборот.

---

## 12. Первый практический шаг

Начать с **настройки инфраструктуры** (Этап 1) – это позволит параллельно работать над бэкендом и фронтендом, используя общий контракт. Фронтенд может использовать моки до готовности API.

После завершения Этапа 1 перейти к Этапу 2 (Core + KB), затем Этапу 3 (RAG), и только после этого – к интерфейсам (Этапы 4–5). Интеграции (Этап 7) – в последнюю очередь.
